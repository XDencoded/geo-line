# Geo map
Добавление точек на карте и проложение маршрута между ними 

что бы запустить проект надо что бы на ПК был установлен пакетный менеджер 
лучше использовать bun 
 что бы его установить надо воспользованить PowerSell

 ```powershell -c "irm bun.sh/install.ps1 | iex"```

 что бы установить библеотеки в проект надо зайти в терминал и по пути в директорию где лежит проект 
 ```cd /map-map # или как называется папка проекта```

 и написать каманду 

 ```bun i ```
 or 

 ```bun install```

 полсле установки пакетов нужно запустить проект камандой 

 ```bun run dev```


# Geo map

Добавление точек на карте и проложение маршрута между ними.

## Необходимое окружение

Для запуска проекта на ПК должен быть установлен пакетный менеджер. Лучше всего использовать **Bun**.

### Установка Bun

Для установки Bun откройте PowerShell и выполните команду:

```powershell
    powershell -c "irm bun.sh/install.ps1 | iex"
```

### Установка зависимостей

- Перейдите в директорию проекта:

```cmd
    cd /путь/к/вашему/проекту  # например, cd /map-map
```

- Установите библиотеки:

```cmd
    bun i
```
или

```cmd
    bun install
```

### Запуск проекта

После установки зависимостей запустите проект командой:

```cmd
    bun run dev
```
 
